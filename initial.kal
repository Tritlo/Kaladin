def f(x){
    y = (x + 3);
    return y; 
};

#Pre : x is int >= 0
#Use : f = fibo(x);
#Post: f is the x-th fibonacci number
def fibo(x){
    if(x == 0){
    	 return 0;
    } else if(x == 1){
      return 1;
    };

    return ( fibo((x-1)) + fibo((x-2)));
};

def infi(){
    if(True or False){
        print("yay");
    } else if(False){
        print("dem");
    };
};


def main(x){
    x = 0;
    while(x < 2){
	print(f(x));
        (x++);
    };
    print(fibo(5));
};
